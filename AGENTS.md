# AGENTS.md

- Use consistent, descriptive names across UI, code, APIs, data, tests, and docs. Complete renames throughout the stack; avoid old/new/temp/v2/fixed.
- After code changes, run `npm run verify`. For selected checks, use `npm run verify -- <checks>`; see `--help` for options.
