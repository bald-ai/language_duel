// Online mock-features prototype. Self-contained; safe to delete with the rest
// of the `mockOnline` feature (lib folder, convex/prototypeRooms.ts, app/mock-online,
// the prototypeRooms schema table, and the homepage button).

export const ROOM_CODE_LENGTH = 4;

// Unambiguous alphabet (no 0/O/1/I) so codes are easy to read out loud.
export const ROOM_CODE_ALPHABET = "ABCDEFGHJKLMNPQRSTUVWXYZ23456789";
